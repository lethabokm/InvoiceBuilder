export interface EntityModel {
  id: number;
  companyName: string;
  contactPerson: string;
  address: string;
  email: string;
  taxId: string;
  active: boolean;
  createdAt: string;
  modifiedAt: string;
}
